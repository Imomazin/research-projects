{
  "protocol_version": "0.1-baseline",
  "status": "pre-results baseline protocol; proposed-method factors intentionally not frozen",
  "seed_policy": {
    "development_seeds": [2026, 2027, 2028],
    "final_seed_count": 20,
    "final_seed_list": "to be generated and frozen before final experiments"
  },
  "federation": {
    "client_counts": [5, 10, 20],
    "participation_rates": [0.5, 1.0],
    "non_iid_levels": ["iid", "mild", "moderate", "severe"],
    "partition_rule": "dataset-specific rule must be fixed before results"
  },
  "aggregation_baselines": [
    "fedavg",
    "fedprox",
    "weighted",
    "median",
    "trimmed-mean",
    "trust-weighted"
  ],
  "byzantine_attacks": [
    {"kind": "sign-flip", "scales": [1, 5, 10]},
    {"kind": "scale", "factors": [5, 10, 50]},
    {"kind": "constant", "values": [-5, 5]},
    {"kind": "gaussian", "std": [1, 5, 10], "seeded": true}
  ],
  "adversarial_client_fractions": [0, 0.1, 0.2, 0.3],
  "privacy_baselines": {
    "modes": ["no-dp", "static-dp", "adaptive-schedule-baseline"],
    "epsilon_budgets": [1, 3, 6, 10],
    "delta_rule": "dataset-size-aware value to be frozen after dataset preprocessing",
    "accounting": "RDP plus explicit per-round schedule record"
  },
  "primary_outcomes": [
    "task utility",
    "convergence rate",
    "worst-client utility",
    "epsilon spent",
    "membership-inference performance",
    "inversion performance",
    "attack-induced utility degradation",
    "runtime",
    "communication volume"
  ],
  "claim_rules": [
    "robust/trust/adaptive mechanisms in this file are baselines until the final novel method passes literature and ablation gates",
    "no value from a development run may be copied into the manuscript results section",
    "final experiments must identify code commit, data manifest, partition manifest and seed"
  ]
}
